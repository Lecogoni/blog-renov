<div class="container">
  <p id="notice"><%= notice %></p>

  <h1>Articles</h1>
  <h2><%= link_to 'New Category', new_category_path %></h2>

  <br>
  <hr>
  <br>
  <% Category.all.each do |cat| %>
    <%= link_to cat.name, articles_path(category: cat.name) %>
  <% end %>
  <br>
  <hr>
  <br>
  <% Category.where(display: true).each do |cat| %>
    <%= link_to cat.name, articles_path(category: cat.name) %>
  <% end %>
  <br>
  <%= link_to 'All', articles_path %>
  <br>
  <hr>
  <br>

  <div class="row">
    <% @articles.each do |article| %>
    <div class="col-4 mb-2">
      <%= link_to (article) do %>
        <div class="card" style="width: 18rem;">
    <%
=begin%>
 <img src="..." class="card-img-top" alt="..."> 
<%
=end%>
          <div class="card-body">
            <h5 class="card-title"><%= article.title %></h5>
            <h5 class="card-title"><%= article.user.full_name %></h5>
            <h6 class="card-title">Catégorie : <%= article.category.name %></h6>
            <p>
              <span class="badge bg-light text-dark"><%= article.likes.count %> <%= (article.likes.count) == 1 ? 'Like' : 'Likes'%></span>
            </p>
            <a href="#" class="btn btn-primary">Go somewhere</a>
            <p class="card-text">
            </p>
          </div>
        </div>
      <% end %>
    </div>
    <% end %>
  </div>

</div>