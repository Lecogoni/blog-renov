<h1>demande de membre</h1>
<table>
    <tr>
      <th>prénom</th>
      <th>nom de famille</th>
      <th>email</th>
      <th>téléphone</th>
      <th></th>
    </tr>

    <% @guests.each do |guest| %>
    <tr>
      <td style="width:20%"><%= guest.first_name.capitalize %></td>
      <td style="width:20%"><%= guest.last_name.capitalize %></td>
      <td style="width:20%"><%= guest.email %></td>
      <td style="width:20%"><%= guest.phone_number %></td>
      <td style="width:20%">
        <%= link_to 'valider la demande', confirm_guest_to_user_guest_path(:guest => guest), method: 'post' %> | 
        <%= link_to 'refuser la demande', refuse_guest_guest_path(:guest => guest), method: 'post', data: { confirm: 'Êtes vous certain de vouloir supprimer cette demande ? Cela est définitive et les infos liée seront supprimées' } %> | 
        envoie email 
      </td>

      <% 
=begin%>
 <td><%= link_to 'voir', user_path(user) %></td> 
<%
=end%>
    </tr>
    <% end %>

  </table>

  <br>
  <br>
  <hr>
  <br>
  <br>